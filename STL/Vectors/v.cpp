#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<int>v;
    v.push_back(1);
    v.push_back(0);
    v.push_back(4);
    v.push_back(2);
    // auto it = v.rend();
    //auto it2 = v.end();
    cout<<*v.begin();
    //cout<<&it<<" "<<&it2;
    return 0;
}